a = 5
b = 100/3
mode(a)
mode(b)
a = "deneme"
mode(a)
# mode() is used to show type of the variable
v1 <- c(1,2,6)
print(v1)
v2 <- c('e','b','t')
c(v1,v2)
# c() is used to combine of variable,vectors, or matrix
savehistory("~/R/lesson5.Rhistory")
